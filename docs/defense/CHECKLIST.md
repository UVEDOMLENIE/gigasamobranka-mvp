# Чек-лист сдачи хакатона — ГигаСамобранка

| Критерий | Макс. | Что у нас | Балл |
|---|---|---|---|
| **Продуктовое исследование и прототип** | 10 | Карточка прототипа заполнена (`docs/PROTOTYPE_CARD.md`), live URL есть, репо публичный. Краткий отчёт — `docs/sprints/*/REPORT.md` + `SAGA.md`. | **10** |
| **Воспроизводимость полного сценария** | 20 | MVP рабочий: загрузка → генерация → редактирование → плеер → ссылка ученику → результаты → печать/экспорт. Smoke green 13/13. Persistence test проходит. | **20** |
| **Качество генерации и обработки материалов** | 20 | Парсинг TXT/PDF/DOCX (mammoth, pdf-parse). Промпт с контекстом (`lib/llm/gigachat.ts`), учитывает difficulty (easy/medium/hard). Форматы: DOCX экспорт. PPTX — best-effort (не полноценный парсер). Scarlex/GigaChat/mock fallback. | **17** (-3 за PPTX) |
| **Гибкость редактирования и настройка параметров** | 10 | Редактор CRUD карточек, drag-reorder, авто-сохранение. Настройки плеера: таймер 0/15/30/60с, 1-3 попытки, shuffle. Нет истории/версий. Нет WYSIWYG. | **8** (-2 за историю/версии) |
| **Форматы вывода, воспроизведения и статистика** | 10 | Персональная ссылка (`/play/[id]`). Печать A4 (`/print/[id]`). DOCX экспорт (`/api/sets/[id]/docx`). Полноэкранный режим плеера. Статистика сессий (`/sets/[id]/results`). | **10** |
| **Техническая реализация и архитектура** | 10 | Схема: клиент → Next.js API → LLM (Scarlex/GigaChat) + SQLite (Vercel Blob). Drizzle ORM. Rate-limit (`LLM_REQUESTS_PER_HOUR`). Код на GitHub с README. | **10** |
| **Безопасность, лимиты и конфиденциальность** | 10 | Rate-limit на LLM (30/час). Owner key в HttpOnly cookie. Данные изолированы по owner_key. Нет публичного доступа к чужим наборам. | **10** |
| **Оформление презентации и документации** | 10 | Структура 8 слайдов (см. `PRESENTATION.md`). Скриншоты 5 PNG. Схема архитектуры (Mermaid). Сравнение с конкурентами. Дорожная карта. Риски. | **10** |

**Итого оценка: ~95 / 100**

## Что доделать для 100%

1. PPTX парсинг — можно добавить `officeparser` или упомянить как ограничение.
2. История/версии карточек — для MVP опционально, но можно сказать "в roadmap".
3. Скринкаст 5-7 мин — **делает пользователь сам**.
